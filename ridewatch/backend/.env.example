CORS_ORIGINS=["http://localhost:3000"]
AWS_REGION=ap-south-1
LOCATION_PROVIDER=aws
# Credentials come from the AWS default credential chain, not this file.
# Tests inject a fake provider; fake mode is not available to normal users.
